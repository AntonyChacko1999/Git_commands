git config --global user.name "AntonyChacko">> configure git with our username

git config --global user.email "antonychacko1999@gmail.com" >> configure git with emailid

configarattion requires while pushing towards a public repositary in github from a local repositary

git status >> used to check the status of .git in the current directory

git init >> to initialaize git in the current directory (helps to track the changes in our directory
 * if we add ,delete a file  it will track it as a change in the directory)

U >> represents untrackwed(file is there not yet committed or not added to staging area)

A >> files that added to staging area

M >> files being modified after commited

git add intro.txt >> intro.txt file added to staging area

git add . >> all files added to staging area

A >> files that added to staging area 

git commit -m "my first commit"

git push -u

git clone <repository url> >>  for cloning a repository from github